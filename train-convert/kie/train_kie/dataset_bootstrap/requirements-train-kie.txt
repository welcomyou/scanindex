accelerate>=0.26.0
optimum[onnx]>=1.23.0
# onnxruntime is required by optimum's fix_dynamic_axes during ONNX export;
# optimum[onnx] extras do not always pull it in.
onnxruntime>=1.17
onnx>=1.16
PyYAML>=6.0
seqeval>=1.2.2
underthesea>=6.8.4
# transformers 4.50+ enforces torch >= 2.6 for .bin checkpoint loading via
# check_torch_load_is_safe() (CVE-2025-32434). Runpod default pod images
# ship torch 2.4.x, and some backbones (vinai/phobert-base-v2) are only
# distributed as .bin (no safetensors variant). Pin to 4.49.x so .bin
# checkpoints still load on torch 2.4 pods. Remove this pin once the pod
# image ships torch >= 2.6 OR the backbones ship safetensors.
transformers>=4.29,<4.50
