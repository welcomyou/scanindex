# kie_core

Runtime KIE helpers shared by the desktop app and KIE viewer.

This package was split out of the old root-level `train_kie` folder so the app
no longer imports from a training workspace. Full training/evaluation history now
lives under `train-convert/kie/train_kie`.
